import { PersmissionDirective } from './persmission.directive';

describe('PersmissionDirective', () => {
  it('should create an instance', () => {
    const directive = new PersmissionDirective();
    expect(directive).toBeTruthy();
  });
});
